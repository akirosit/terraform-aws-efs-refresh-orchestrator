{
    "Comment": "Input for the EFS State Machine",
    "SourceEFSId": "${SourceEFSId}",
    "TmpEFSName": "${EFSName}-restore",
    "EFSName": "${EFSName}",
    "AWSBackupRoleArn": "${AWSBackupRoleArn}",
    "ItemsToRestore": ${jsonencode(ItemsToRestore)},
    "TagApplication": "${AppName}",
    "TagEnvironment": "preprod",
    "DynamoDBTableName": "${DynamoDBTable}",
    "SNSTopicArn": "${SnsTopicArn}",
    "SNSSubject": "Application ${AppName}: restore of the EFS ${SourceEFSId} [COMPLETED]",
    "SNSMessage": "The restore of the database instance ${EFSName} from the source ${SourceEFSId} has been completed successfully.",
    "SNSSubjectFailure": "Application ${AppName}: restore of the database instance ${SourceEFSId} [FAILED]",
    "SNSMessageFailure": "The restore of the database instance ${EFSName} from the source ${SourceEFSId} has failed."
}